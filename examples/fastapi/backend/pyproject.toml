[project]
name = "backend"
version = "0.1.0"
description = "FastAPI backend for NLP Engine"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "fastapi[standard]>=0.135.1",
    "python-dotenv>=1.0.1",
]
